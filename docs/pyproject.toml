[project]
name = "lumen-docs"
version = "0.1.0"
description = "The documentation for the Lumen UI framework."
requires-python = ">=3.13"
dependencies = []

[dependency-groups]
dev = [
    "zensical>=0.0.47",
]
