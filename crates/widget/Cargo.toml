[package]
name = "lumen-widget"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
rust-version.workspace = true
description = "Widget trait + Attributes type - the surface implemented by `#[derive(Widget)]` (see lumen-widget-macros)."
keywords = ["ui", "gui", "widgets", "components", "lumen"]
categories = ["gui"]

[dependencies]
lumen-core = { path = "../core", version = "0.0.3" }
bevy_ecs.workspace = true

[dev-dependencies]
lumen-widget-macros = { path = "../widget-macros" }
