[package]
name = "lumen-primitives"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
rust-version.workspace = true
description = "Headless primitives with per-OS native physics (Scroll, Drag, Press)."
keywords = ["ui", "gui", "scroll", "gestures", "lumen"]
categories = ["gui"]
# Not published: `lumenc` and `lumenui` are the only crates.io packages.
publish = false

[dependencies]
lumen-core = { workspace = true }
lumen-input = { workspace = true }
lumen-widget = { workspace = true }
lumen-widget-macros = { workspace = true }
bevy_ecs.workspace = true
glam.workspace = true
