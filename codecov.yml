# Coverage is a report, not a gate: the test suite decides whether a pull
# request passes. Both Codecov statuses are informational, so they measure the
# change and comment on it without ever failing a pull request.

coverage:
  status:
    project:
      default:
        informational: true
    patch:
      default:
        informational: true
