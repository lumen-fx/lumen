// Publishes what `t()` and `locale()` answer while the app starts, so a test
// can read which language a run was in.
import "lumen.cdl";

fn main() {}

fn on_start() {
    lumen::signal_set("greeting", lumen::t("greeting"));
    lumen::signal_set("running_in", lumen::locale());
}
